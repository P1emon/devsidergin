@model DevSidergin.Entities.ThongBao
@{
    ViewData["Title"] = "Tạo thông báo mới";
    var sliders = ViewData["Sliders"] as List<MyEStore.Models.Slider> ?? new List<MyEStore.Models.Slider>();
}

<div class="min-h-screen bg-gray-100 py-8 px-4 sm:px-6 lg:px-8">
    <div class="max-w-7xl mx-auto">
        <!-- Header -->
        <div class="bg-white shadow-lg rounded-xl overflow-hidden mb-6">
            <div class="bg-gradient-to-r from-blue-700 to-indigo-700 px-6 py-4">
                <h1 class="text-2xl font-semibold text-white text-center">
                    <i class="fas fa-plus mr-2"></i>Tạo thông báo mới
                </h1>
            </div>
        </div>

        <!-- Form -->
        <div class="bg-white shadow-lg rounded-xl p-6">
            <form asp-action="Create" method="post" class="space-y-6">
                <div asp-validation-summary="ModelOnly" class="text-red-600 text-sm"></div>

                <!-- Slider Selection -->
                <div>
                    <label class="block text-sm font-medium text-gray-700">Chọn Slider <span class="text-red-500">*</span></label>
                    <select id="sliderSelect" class="w-full px-3 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 bg-gray-50 text-sm">
                        <option value="">-- Chọn slider --</option>
                        @foreach (var slider in sliders)
                        {
                            <option value="@slider.MaSlider" data-title="@slider.TieuDe" data-content="@slider.MoTa">@slider.TieuDe</option>
                        }
                    </select>
                </div>

                <!-- Title -->
                <div>
                    <label asp-for="TieuDe" class="block text-sm font-medium text-gray-700">Tiêu đề <span class="text-red-500">*</span></label>
                    <input asp-for="TieuDe" class="w-full px-3 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 bg-gray-50 text-sm" placeholder="Nhập tiêu đề thông báo" required />
                    <span asp-validation-for="TieuDe" class="text-red-600 text-xs"></span>
                </div>

                <!-- Content -->
                <div>
                    <label asp-for="NoiDung" class="block text-sm font-medium text-gray-700">Nội dung <span class="text-red-500">*</span></label>
                    <textarea asp-for="NoiDung" rows="4" class="w-full px-3 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 bg-gray-50 text-sm" placeholder="Nhập nội dung thông báo" required></textarea>
                    <span asp-validation-for="NoiDung" class="text-red-600 text-xs"></span>
                </div>

                <!-- Hidden Fields -->
                <input type="hidden" asp-for="MaSlider" id="maSlider" />
                <input type="hidden" asp-for="MaKh" id="maKh" />
                <input type="hidden" asp-for="MaMv" id="maMv" />

                <!-- Submit Button -->
                <div class="flex justify-end">
                    <button type="submit" class="inline-flex items-center px-4 py-2 rounded-lg bg-blue-600 text-white hover:bg-blue-700 transition-colors text-sm">
                        <i class="fas fa-paper-plane mr-2"></i>Gửi thông báo
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.getElementById('sliderSelect').addEventListener('change', function() {
            const selectedOption = this.options[this.selectedIndex];
            if (selectedOption.value) {
                document.getElementById('TieuDe').value = selectedOption.dataset.title;
                document.getElementById('NoiDung').value = selectedOption.dataset.content;
                document.getElementById('maSlider').value = selectedOption.value;
            }
        });
    </script>
} 